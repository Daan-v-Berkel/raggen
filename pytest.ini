[pytest]
markers =
    integration: marks tests that load real models or hit real external services
    requires_torch: marks tests that need sentence-transformers installed
    requires_pgvector: marks tests that need PostgreSQL with pgvector
